import react from "react";
import { Grid, Button, Typography, Card } from "@material-ui/core";

const SongList = () => {
  return (
    <Grid item className='playlist-area-container'>
      <Typography>Song List Area</Typography>
    </Grid>
  );
};

export default SongList;
